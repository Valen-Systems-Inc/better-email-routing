{
  "name": "better-email-routing",
  "version": "1.0.0",
  "private": true,
  "description": "Local Cloudflare Email Service composer.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check worker/src/index.js",
    "test": "node --test worker/src/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
